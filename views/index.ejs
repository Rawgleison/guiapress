<%- include('./partials/header.ejs') %> 
<%- include('./partials/navbar.ejs') %>
<body>
  <div class="container">
    <br>
    <div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">Bem vindo</h1>
        <p class="col-md-8 fs-4">Este é um simples blog do Raul.</p>
      </div>
      </div>
      <div>
        <% articles.forEach(article => { %>
          <div class="card">
            <div class="card-header">
              <h4 class="card-title"> 
                <%= article.title %> 
              </h4>
            </div>
            <div class="card-body">
              <a name="<%= article.id %>" id="<%= article.id %>" class="btn btn-primary" href="/articles/article/<%= article.slug %> " role="button">Ler Artigo</a>
            </div>
          </div>
          <br>
        <% }) %>
      </div>
      <div class="d-flex flex-row-reverse">
            <a href="/home/2"> proximo >>>> </a>
      </div>
  </div>
</body>
<%- include('./partials/footer.ejs') %>
